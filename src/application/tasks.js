export function createtask(title,description,duedate,priority,notes,checklist){
    return{title,
            description,
            duedate,
            priority,
            notes,
            checklist,
    }
}
